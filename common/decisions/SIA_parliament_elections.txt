SIA_parliament_elections = {
	SIA_2023_elections = {
		icon = generic_crisis
		allowed = {
			tag = SIA
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 180
		timeout_effect = {
			SIA_decide_election_winner_2023 = yes
		}
		complete_effect = {
			
		}
	}
	SIA_2027_elections = {
		icon = generic_crisis
		allowed = {
			tag = SIA
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 180
		timeout_effect = {
			SIA_decide_election_winner_2027 = yes
		}
		complete_effect = {
			
		}
	}
	SIA_other_parties_campaigning = {
		icon = generic_crisis
		allowed = {
			tag = SIA
		}
		available = {
			NOT = { has_country_flag = SIA_ongoing_elections }
		}
		activation = {
			always = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 5
		fixed_random_seed = no
		timeout_effect = {
			if = {
				limit = {
					has_country_flag = SIA_2023_elections
				}
				SIA_roll_other_party_campaigning_2023 = yes
			}
			else_if = {
				limit = {
					has_country_flag = SIA_2027_elections
				}
				#SIA_roll_other_party_campaigning_2027 = yes
			}
			activate_mission = SIA_other_parties_campaigning
			if = {
				limit = {
					has_country_flag = SIA_2023_elections
					check_variable = { undecided_votes = 0 }
				}
				hidden_effect = {
					SIA_roll_2nd_target_2023 = yes
				}
			}
		}
		complete_effect = {
			
		}
	}
	SIA_activate_prayut_2023 = {
		icon = generic_crisis
		available = {
			always = yes
		}
		visible = {
			always = yes
			is_debug = yes
		}
		complete_effect = {
			activate_mission = SIA_2023_elections
			activate_mission = SIA_other_parties_campaigning
			set_country_flag = SIA_ongoing_elections
			set_country_flag = SIA_utn_active
			country_event = thai_elections.2
			country_event = thai_elections.3
		}
	}
	SIA_activate_election_2027 = {
		icon = generic_crisis
		available = {
			always = yes
		}
		visible = {
			always = yes
			is_debug = yes
		}
		complete_effect = {
			activate_mission = SIA_2027_elections
			activate_mission = SIA_other_parties_campaigning
			set_country_flag = SIA_ongoing_elections
			set_country_flag = SIA_utn_active
			country_event = thai_elections.2
			country_event = thai_elections.7
		}
	}
	SIA_campaign_for_the_utn = {
		icon = generic_crisis
		available = {
			custom_trigger_tooltip = {
				check_variable = { undecided_votes > 0 }
				tooltip = SIA_undecided_votes_available
			}
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			has_completed_focus = SIA_riding_with_uncle_tuu
		}
		cost = 25
		days_remove = 14
		complete_effect = {
			set_temp_variable = { target = 11 }
			set_temp_variable = { vote_change = 2 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_smear_campaign = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			has_country_flag = SIA_2023_elections
			check_variable = { SIA_election_playing_as = 11 }
		}
		cost = 50
		days_remove = 20
		fixed_random_seed = no
		complete_effect = {
			random_list = {
				1 = {
					modifier = {
						factor = 0
						check_variable = { 1:party_votes < 5 }
					}
					set_temp_variable = { target = 1 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
				1 = {
					modifier = {
						factor = 0
						check_variable = { 2:party_votes < 12 }
					}
					set_temp_variable = { target = 2 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
				1 = {
					modifier = {
						factor = 0
						check_variable = { 3:party_votes < 10 }
					}
					set_temp_variable = { target = 3 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
				1 = {
					modifier = {
						factor = 0
						check_variable = { 4:party_votes < 6 }
					}
					set_temp_variable = { target = 4 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
				1 = {
					modifier = {
						factor = 0
						check_variable = { 6:party_votes < 8 }
					}
					set_temp_variable = { target = 6 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
				1 = {
					modifier = {
						factor = 0
						check_variable = { 9:party_votes < 8 }
					}
					set_temp_variable = { target = 9 }
					set_temp_variable_to_random = {
						var = vote_change
						min = -5
						max = -3
						integer = no
					}
					SIA_elections_increase_votes_by_undecided = yes
					
				}
			}
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_berate_our_opponents = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			has_country_flag = SIA_2023_elections
			check_variable = { SIA_election_playing_as = 11 }
			has_completed_focus = SIA_riding_with_uncle_tuu
		}
		cost = 50
		days_remove = 25
		fire_only_once = yes
		complete_effect = {
			set_temp_variable = { target = 1 }
			set_temp_variable = { vote_change = -2 }
			SIA_elections_increase_votes_by_undecided = yes
			
			set_temp_variable = { target = 2 }
			set_temp_variable = { vote_change = -2 }
			SIA_elections_increase_votes_by_undecided = yes
			
			set_temp_variable = { target = 3 }
			set_temp_variable = { vote_change = -2 }
			SIA_elections_increase_votes_by_undecided = yes
			
			set_temp_variable = { target = 4 }
			set_temp_variable = { vote_change = -2 }
			SIA_elections_increase_votes_by_undecided = yes
			
			set_temp_variable = { target = 6 }
			set_temp_variable = { vote_change = -2 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_conduct_fundraising = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
		}
		days_remove = 15
		complete_effect = {
			add_political_power = 50
			set_temp_variable = { target = SIA_election_playing_as }
			set_temp_variable = { vote_change = -3 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_showcase_prayuts_achievements = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			has_country_flag = SIA_2023_elections
			check_variable = { SIA_election_playing_as = 11 }
			has_completed_focus = SIA_riding_with_uncle_tuu
		}
		cost = 30
		days_remove = 20
		fire_only_once = yes
		complete_effect = {
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			set_temp_variable = { target = 11 }
			set_temp_variable = { vote_change = 3 }
			SIA_elections_increase_votes_by_undecided = yes
			clr_country_flag = SIA_active_election_decision
		}
	}
}
