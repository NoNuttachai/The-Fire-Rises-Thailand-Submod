SIA_parliament_elections = {
	SIA_2023_elections = {
		icon = generic_crisis
		allowed = {
			tag = SIA
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 180
		timeout_effect = {
			SIA_decide_election_winner = yes
		}
		complete_effect = {
			
		}
	}
	SIA_other_parties_campaigning = {
		icon = generic_crisis
		allowed = {
			tag = SIA
		}
		available = {
			always = no
		}
		activation = {
			always = no
		}
		is_good = no
		selectable_mission = no
		days_mission_timeout = 10
		timeout_effect = {
			SIA_roll_other_party_campaigning = yes
			activate_mission = SIA_other_parties_campaigning
		}
		complete_effect = {
			
		}
	}
	SIA_activate_prayut_2023 = {
		icon = generic_crisis
		available = {
			always = yes
		}
		visible = {
			always = yes
		}
		complete_effect = {
			activate_mission = SIA_2023_elections
			activate_mission = SIA_other_parties_campaigning
			SIA_setup_elections = yes
			set_country_flag = SIA_ongoing_elections
			set_country_flag = SIA_utn_active
		}
	}
	SIA_campaign_for_your_party = {
		icon = generic_crisis
		available = {
			custom_trigger_tooltip = {
				check_variable = { undecided_votes > 0 }
				tooltip = SIA_undecided_votes_available
			}
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		cost = 25
		days_remove = 14
		complete_effect = {
			set_temp_variable = { target = SIA_election_playing_as }
			set_temp_variable = { vote_change = 3.5 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_smear_campaign_against_pheu = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			check_variable = { SIA_election_playing_as = 11 }
		}
		cost = 25
		days_remove = 14
		complete_effect = {
			set_temp_variable = { target = 2 }
			set_temp_variable = { vote_change = -3.5 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_smear_campaign_against_utn = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			check_variable = { SIA_election_playing_as = 2 }
		}
		cost = 25
		days_remove = 14
		complete_effect = {
			set_temp_variable = { target = 11 }
			set_temp_variable = { vote_change = -3.5 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_exchange_votes_for_pp = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
		}
		days_remove = 20
		complete_effect = {
			add_political_power = 50
			set_temp_variable = { target = SIA_election_playing_as }
			set_temp_variable = { vote_change = -3 }
			SIA_elections_increase_votes_by_undecided = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_attract_pp_voters = {
		icon = generic_crisis
		available = {
			NOT = {
				has_country_flag = SIA_active_election_decision
			}
		}
		visible = {
			has_country_flag = SIA_ongoing_elections
			check_variable = { SIA_election_playing_as = 11 }
		}
		days_remove = 20
		complete_effect = {
			set_temp_variable = { 1st_target = 11 }
			set_temp_variable = { 2nd_target = 1 }
			set_temp_variable = { vote_change = 2.5 }
			SIA_elections_targeted_vote_increase = yes
			set_country_flag = SIA_active_election_decision
		}
		remove_effect = {
			clr_country_flag = SIA_active_election_decision
		}
	}
	SIA_play_as_prayut = {
		icon = generic_crisis
		available = {
			always = yes
		}
		visible = {
			always = yes
		}
		complete_effect = {
			set_variable = { SIA_election_playing_as = 11 }
		}
	}
	SIA_play_as_pheu = {
		icon = generic_crisis
		available = {
			always = yes
		}
		visible = {
			always = yes
		}
		complete_effect = {
			set_variable = { SIA_election_playing_as = 2 }
		}
	}
}
